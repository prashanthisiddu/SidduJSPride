function TickerChange(executionContext) {
    debugger;

        var formContext = executionContext.getFormContext();
   var reportingmanager = formContext.ui.tabs.get("tab1");
  var reportingmanagersec = reportingmanager.sections.get("tab_6_section_1"); 
 var employeereportingtype = formContext.ui.tabs.get("tab_7");
    var employeereportingtypesec = employeereportingtype.sections.get("tab_7_section_1");
  var team = formContext.ui.tabs.get("tab_8");
    var teamrsec = team.sections.get("tab_8_section_1");
    var dojo = formContext.ui.tabs.get("tab_10");
    var dojosec = dojo.sections.get("tab_10_section_1");
var workloc = formContext.ui.tabs.get("tab_11");
    var worklocsec = workloc.sections.get("tab_11_section_1");

    var workmode = formContext.ui.tabs.get("tab_12");
    var workmodesec = workmode.sections.get("tab_12_section_1");
    var costallo = formContext.ui.tabs.get("tab_13");
    var costallosec = costallo.sections.get("tab_13_section_1");
    var salaryrevision = formContext.ui.tabs.get("tab_14");
    var salaryrevisionsec = salaryrevision.sections.get("tab_14_section_1");
      var applicationselect = 0;
 
    try {
      var  applicationselect = formContext.getAttribute("pg_application").getSelectedOption().value;
var  datachangetype = formContext.getAttribute("pg_datachangetype").getSelectedOption().value;
    }
    catch
    {
        var applicationselect = 0;
    }
   
    if (applicationselect == 10 && datachangetype == 1) {
      reportingmanager.setVisible(true);
      employeereportingtype.setVisible(false);
      team.setVisible(false);
      dojo.setVisible(false);
      workmode.setVisible(false);
      costallo.setVisible(false);
      workloc.setVisible(false);
      salaryrevision.setVisible(false);
       
    }
    else if (applicationselect == 10 && datachangetype == 140310001) {
        reportingmanager.setVisible(false);
      employeereportingtype.setVisible(true);
      team.setVisible(false);
      dojo.setVisible(false);
      workmode.setVisible(false);
      costallo.setVisible(false);
      workloc.setVisible(false);
      salaryrevision.setVisible(false);

    }
    else if (applicationselect == 10 && datachangetype == 140310002) {
        reportingmanager.setVisible(false);
        employeereportingtype.setVisible(false);
        team.setVisible(true);
        dojo.setVisible(false);
        workmode.setVisible(false);
        costallo.setVisible(false);
        workloc.setVisible(false);
        salaryrevision.setVisible(false);
    }
    else if (applicationselect == 10 && datachangetype == 140310003) {
        reportingmanager.setVisible(false);
        employeereportingtype.setVisible(false);
        team.setVisible(false);
        dojo.setVisible(true);
        workmode.setVisible(false);
        costallo.setVisible(false);
        workloc.setVisible(false);
        salaryrevision.setVisible(false);
    }
    else if (applicationselect == 10 && datachangetype == 140310004) {
        reportingmanager.setVisible(false);
      employeereportingtype.setVisible(false);
      team.setVisible(false);
      dojo.setVisible(false);
      workmode.setVisible(false);
      costallo.setVisible(false);
      workloc.setVisible(true);
      salaryrevision.setVisible(false);
    }
    else if (applicationselect == 10 && datachangetype == 140310005) {
        reportingmanager.setVisible(false);
        employeereportingtype.setVisible(false);
        team.setVisible(false);
        dojo.setVisible(false);
        workmode.setVisible(true);
        costallo.setVisible(false);
        workloc.setVisible(false);
        salaryrevision.setVisible(false);
    }
    else if (applicationselect == 10 && datachangetype == 140310006) {
        reportingmanager.setVisible(false);
      employeereportingtype.setVisible(false);
      team.setVisible(false);
      dojo.setVisible(false);
      workmode.setVisible(false);
      costallo.setVisible(true);
      workloc.setVisible(false);
      salaryrevision.setVisible(false);
    }
    else if (applicationselect == 10 && datachangetype == 140310007) {
       reportingmanager.setVisible(false);
      employeereportingtype.setVisible(false);
      team.setVisible(false);
      dojo.setVisible(false);
      workmode.setVisible(false);
      costallo.setVisible(false);
      workloc.setVisible(false);
      salaryrevision.setVisible(true);
    }
}


function demo(executionContext) {
debugger;

    var formContext = executionContext.getFormContext();
    var application = formContext.getAttribute("pg_application").getValue();
    // var applicationselect = formcontext.getAttribute("pg_application").getSelectedOption().value;
    var origin = formContext.getAttribute("caseorigincode").getValue();
    var casetitle = formContext.getAttribute("title").getValue();
    if (application == 140310017 && origin == 140310000) {

        if (casetitle.includes("IT")) {
            var x = formContext.getAttribute("pg_assetissued").getValue();    
            var y = formContext.getAttribute("pg_databackupcompleted").getValue();
            var z = formContext.getAttribute("pg_assetreceived").getValue();
         var a= formContext.getAttribute("pg_azureadaccessdeactivated").getValue();    
            var b = formContext.getAttribute("pg_emaildeactivated").getValue();
            var c = formContext.getAttribute("pg_emailforwarding").getValue();
         var demo=formContext.getControl("pg_assetissued");
         var mama=formContext.getControl("pg_databackupcompleted");
       var mimi= formContext.getControl("pg_assetreceived");
     var aada=formContext.getControl("pg_azureadaccessdeactivated");
        var emaildeativ=formContext.getControl("pg_emaildeactivated");
       var emailforw= formContext.getControl("pg_emailforwarding");
         if (x  ==1 ||x== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             demo.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           demo.clearNotification("validation");
          }

         if (y  ==1 || y== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             mama.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           mama.clearNotification("validation");
          }
          if (z  ==1  || z== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             mimi.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         mimi.clearNotification("validation");
          }
 if (a  ==1  || a== null) {
            formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             aada.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           aada.clearNotification("validation");
          }
         if (b  ==1  || b== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             emaildeativ.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           emaildeativ.clearNotification("validation");
          }
          if (c  ==1  || c== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             emailforw.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         emailforw.clearNotification("validation");
          }
}
    
     else if(casetitle.includes("Admin")){
      
          var a= formContext.getAttribute("pg_assetpickupcoordination").getValue();      
           var b=formContext.getAttribute("pg_assetreceivedhandedovertoit").getValue();
           var c=formContext.getAttribute("pg_idaccesscardreceived").getValue();
           var d= formContext.getAttribute("pg_accesscarddeactivated").getValue();      
           var e=formContext.getAttribute("pg_drawerkeyreceived").getValue();
           var f=formContext.getAttribute("pg_datacarddongle").getValue();
            var g=formContext.getAttribute("pg_mobilephonesim").getValue();
           var h=formContext.getAttribute("pg_medicalinsuranceterminated").getValue();
      var apc=formContext.getControl("pg_assetpickupcoordination");
      var arho=formContext.getControl("pg_assetreceivedhandedovertoit");
        var iacr=formContext.getControl("pg_idaccesscardreceived");
         var aca=formContext.getControl("pg_accesscarddeactivated");
            var dkr=formContext.getControl("pg_drawerkeyreceived");
           var dc=formContext.getControl("pg_datacarddongle");
      var mobile=formContext.getControl("pg_mobilephonesim");
           var med=formContext.getControl("pg_medicalinsuranceterminated");
   if (a ==1  || a== null) {
            formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             apc.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           apc.clearNotification("validation");
          }
if (b ==1  || b== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            arho.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
          arho.clearNotification("validation");
          }
       if (c  ==1  || c== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            iacr.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         iacr.clearNotification("validation");
          }
        if (d  ==1  || d== null) {
            formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             aca.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           aca.clearNotification("validation");
          }
         if (e  ==1  || e== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            dkr.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           dkr.clearNotification("validation");
          }
       if (f  ==1  || f== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             dc.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          dc.clearNotification("validation");
      }
      if (g  ==1  || g== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             mobile.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          mobile.clearNotification("validation");
      }
 if (h ==1  || h== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             med.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          med.clearNotification("validation");
      }
  }     

  else if(casetitle.includes("Employee Development")){
      var a= formContext.getAttribute("pg_deactivatexyme").getValue();     
      var b=formContext.getAttribute("pg_updatebonuseligibilityinbonusfile").getValue();
      var deactivate= formContext.getControl("pg_deactivatexyme");    
      var updatebonus=formContext.getControl("pg_updatebonuseligibilityinbonusfile");
    if (a  ==1  || a== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             deactivate.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          deactivate.clearNotification("validation");
      }
 if (b ==1  || b== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             updatebonus.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          updatebonus.clearNotification("validation");
      }
  }
   else if(casetitle.includes("Learning & Development")){
    var a= formContext.getAttribute("pg_completeactivetrainingsinlms").getValue();      
      var b=formContext.getAttribute("pg_deactivatelmsaccess").getValue();
      var completeactive= formContext.getControl("pg_completeactivetrainingsinlms");    
      var deactivelms=formContext.getControl("pg_deactivatelmsaccess");
           if (a  ==1  || a== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             completeactive.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         completeactive.clearNotification("validation");
          }
         if (b ==1  || b== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            deactivelms.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
          deactivelms.clearNotification("validation");
          }
   }
   else if(casetitle.includes("Finance")){
       var a= formContext.getAttribute("pg_updatetallywithenddate").getValue();     
      var b=formContext.getAttribute("pg_moveemployeefrompayrolltoff").getValue();
      var c=formContext.getAttribute("pg_receiveffclearancefromhr").getValue();
      var d= formContext.getAttribute("pg_processff").getValue();      
      var e=formContext.getAttribute("pg_processgratuity").getValue();
      var f=formContext.getAttribute("pg_processleaveencashment").getValue();
      var g=formContext.getAttribute("pg_processdeductionsforlopadjustments").getValue();
      var h=formContext.getAttribute("pg_publishffpayslip").getValue();
     var i=formContext.getAttribute("pg_updatedateofexitinpfportal").getValue();
  var apc=formContext.getControl("pg_updatetallywithenddate");
      var arho=formContext.getControl("pg_moveemployeefrompayrolltoff");
        var iacr=formContext.getControl("pg_receiveffclearancefromhr");
         var aca=formContext.getControl("pg_processff");
            var dkr=formContext.getControl("pg_processgratuity");
           var dc=formContext.getControl("pg_processleaveencashment");
      var mobile=formContext.getControl("pg_processdeductionsforlopadjustments");
           var med=formContext.getControl("pg_publishffpayslip");
   var ink=formContext.getControl("pg_updatedateofexitinpfportal");
   if (a ==1  || a== null) {
            formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             apc.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           apc.clearNotification("validation");
          }
if (b ==1  || b== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            arho.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
          arho.clearNotification("validation");
          }
       if (c  ==1  || c== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            iacr.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         iacr.clearNotification("validation");
          }
        if (d  ==1  || d== null) {
            formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             aca.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           aca.clearNotification("validation");
          }
         if (e  ==1  || e== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            dkr.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           dkr.clearNotification("validation");
          }
       if (f  ==1  || f== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             dc.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          dc.clearNotification("validation");
      }
      if (g  ==1  || g== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             mobile.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          mobile.clearNotification("validation");
      }
 if (h ==1  || h== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             med.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          med.clearNotification("validation");
      }
 if (i ==1  || i== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             ink.setNotification("pls enter the valid data", "validation");
             }
            else  {
               formContext.ui.clearFormNotification("validation");
          ink.clearNotification("validation");
      }
   }
    else if(casetitle.includes("Employee Engagement")){
      var a= formContext.getAttribute("pg_updatedeierglist").getValue();     
      var b=formContext.getAttribute("pg_updateclashoftitanshouselistjosh").getValue();
      var c=formContext.getAttribute("pg_conductexitinterview").getValue();
      var d= formContext.getAttribute("pg_disabledojostoreaccess").getValue();      
      var e=formContext.getAttribute("pg_cancelopenordersinthequeue").getValue();
      var updatedei= formContext.getControl("pg_updatedeierglist");    
      var updateclas=formContext.getControl("pg_updateclashoftitanshouselistjosh");
     var con=formContext.getControl("pg_conductexitinterview");
     var doj= formContext.getControl("pg_disabledojostoreaccess");    
     var cancel=formContext.getControl("pg_cancelopenordersinthequeue");
          if (a  ==1 || a== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             updatedei.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         updatedei.clearNotification("validation");
          }
         if (b ==1  || b== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            updateclas.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
          updateclas.clearNotification("validation");
          }
 if (c  ==1  || c== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            con.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
         con.clearNotification("validation");
          }
        if (d  ==1 || d== null) {
            formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
             doj.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           doj.clearNotification("validation");
          }
         if (e  ==1  || e== null) {
             formContext.ui.setFormNotification("pls enter the valid data", "ERROR", "validation");
            cancel.setNotification("pls enter the valid data", "validation");
             }
            else  {
              formContext.ui.clearFormNotification("validation");
  
           cancel.clearNotification("validation");
          }
     }
   }
}

               






function review(primaryControl) {
debugger;
var formContext=primaryControl;
    var application = formContext.getAttribute("pg_application").getValue();
    // var applicationselect = formcontext.getAttribute("pg_application").getSelectedOption().value;
    var origin = formContext.getAttribute("caseorigincode").getValue();
    var casetitle = formContext.getAttribute("title").getValue();
    if (application == 140310017 && origin == 140310000) {

        if (casetitle.includes("IT")) {
            var assetissued = formContext.getAttribute("pg_assetissued").getValue();    
            var databackupcompleted = formContext.getAttribute("pg_databackupcompleted").getValue();
            var assetreceived = formContext.getAttribute("pg_assetreceived").getValue();
         var azureadaccessdeactivated= formContext.getAttribute("pg_azureadaccessdeactivated").getValue();    
            var emaildeactivated = formContext.getAttribute("pg_emaildeactivated").getValue();
            var emailforwarding= formContext.getAttribute("pg_emailforwarding").getValue;

      if (assetissued ==0 || 2 && databackupcompleted ==0 || 2 && assetreceived  ==0 || 2){
          return true;
        }
       else{
          return false;

         }

}
}
 if (databackupcompleted ==0 || 2){
          return true;
        }
       else{
          return false;

         }

       }
 if (assetreceived  ==0 || 2){
          return true;
        }
       else{
          return false;

         }

       }
       
     else if(casetitle.includes("Admin")){
      
          var a= formContext.getAttribute("pg_assetpickupcoordination").getValue();      
           var b=formContext.getAttribute("pg_assetreceivedhandedovertoit").getValue();
           var c=formContext.getAttribute("pg_idaccesscardreceived").getValue();
           var d= formContext.getAttribute("pg_accesscarddeactivated").getValue();      
           var e=formContext.getAttribute("pg_drawerkeyreceived").getValue();
           var f=formContext.getAttribute("pg_datacarddongle").getValue();
            var g=formContext.getAttribute("pg_mobilephonesim").getValue();
           var h=formContext.getAttribute("pg_medicalinsuranceterminated").getValue();
 if (a ||b||c||d  || e|| f||g||h ==1){
          return true;
        }
       else{
          return false;

         }
         
  }     

  else if(casetitle.includes("Employee Development")){
      var a= formContext.getAttribute("pg_deactivatexyme").getValue();     
      var b=formContext.getAttribute("pg_updatebonuseligibilityinbonusfile").getValue();
        if (a||b==1){
 return true;
        }
       else{
          return false;

         }
   }
  
   else if(casetitle.includes("Learning & Development")){
    var a= formContext.getAttribute("pg_completeactivetrainingsinlms").getValue();      
      var b=formContext.getAttribute("pg_deactivatelmsaccess").getValue();
     if (a||b==1){
 return true;
        }
       else{
          return false;

         }
   }
   else if(casetitle.includes("Finance")){
       var a= formContext.getAttribute("pg_updatetallywithenddate").getValue();     
      var b=formContext.getAttribute("pg_moveemployeefrompayrolltoff").getValue();
      var c=formContext.getAttribute("pg_receiveffclearancefromhr").getValue();
      var d= formContext.getAttribute("pg_processff").getValue();      
      var e=formContext.getAttribute("pg_processgratuity").getValue();
      var f=formContext.getAttribute("pg_processleaveencashment").getValue();
      var g=formContext.getAttribute("pg_processdeductionsforlopadjustments").getValue();
      var h=formContext.getAttribute("pg_publishffpayslip").getValue();
     var i=formContext.getAttribute("pg_updatedateofexitinpfportal").getValue();
   if (a ||b||c||d  || e|| f||g||h || i==1){
          return true;
        }
       else{
          return false;

         }  
   }
    else if(casetitle.includes("Employee Engagement")){
      var a= formContext.getAttribute("pg_updatedeierglist").getValue();     
      var b=formContext.getAttribute("pg_updateclashoftitanshouselistjosh").getValue();
      var c=formContext.getAttribute("pg_conductexitinterview").getValue();
      var d= formContext.getAttribute("pg_disabledojostoreaccess").getValue();      
      var e=formContext.getAttribute("pg_cancelopenordersinthequeue").getValue();
      if (a ||b||c||d  || e ==1){
          return true;
        }
       else{
          return false;
      }     
     }
   }
}




function siddu(executionContext) {

debugger;

    var formContext = executionContext.getFormContext();
    var application = formContext.getAttribute("pg_application").getValue();
    // var applicationselect = formcontext.getAttribute("pg_application").getSelectedOption().value;
    var origin = formContext.getAttribute("caseorigincode").getValue();
    var casetitle = formContext.getAttribute("title").getValue();
    if (application == 140310017 && origin == 140310000) {

        if (casetitle.includes("IT")) {
            var assetissued = formContext.getAttribute("pg_assetissued").getValue();    
            var data= formContext.getAttribute("pg_databackupcompleted").getValue();
            var assetreceived = formContext.getAttribute("pg_assetreceived").getValue();
         var azureadaccessdeactivated= formContext.getAttribute("pg_azureadaccessdeactivated").getValue();    
            var emaildeactivated = formContext.getAttribute("pg_emaildeactivated").getValue();
            var emailforwarding = formContext.getAttribute("pg_emailforwarding").getValue();
        
       if(assetissued==1 || assetissued==null){
              formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
       if(data==1 || data==null){
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
      if (assetreceived  ==1  || assetreceived== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
    if (azureadaccessdeactivated ==1  || azureadaccessdeactivated== null) {
           formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
        if (emaildeactivated  ==1  || emaildeactivated== null) {
              formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
         if (emailforwarding  ==1  || emailforwarding== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
}
    
     else if(casetitle.includes("Admin")){
      
          var assetpickupcoordination= formContext.getAttribute("pg_assetpickupcoordination").getValue();      
           var assetreceivedhandedovertoit=formContext.getAttribute("pg_assetreceivedhandedovertoit").getValue();
           var idaccesscardreceived=formContext.getAttribute("pg_idaccesscardreceived").getValue();
           var accesscarddeactivated= formContext.getAttribute("pg_accesscarddeactivated").getValue();      
           var drawerkeyreceived=formContext.getAttribute("pg_drawerkeyreceived").getValue();
           var datacarddongle=formContext.getAttribute("pg_datacarddongle").getValue();
            var mobilephonesim=formContext.getAttribute("pg_mobilephonesim").getValue();
           var medicalinsuranceterminated=formContext.getAttribute("pg_medicalinsuranceterminated").getValue();
    
   if (assetpickupcoordination ==1  || assetpickupcoordination== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
  if (assetreceivedhandedovertoit ==1  || assetreceivedhandedovertoit== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
         formContext.getAttribute("statuscode").setValue(5);
          }
       if (idaccesscardreceived  ==1  || idaccesscardreceived== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
        if (accesscarddeactivated  ==1  || accesscarddeactivated== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
      formContext.getAttribute("statuscode").setValue(5);
          }
         if (drawerkeyreceived  ==1  || drawerkeyreceived== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
      if (datacarddongle  ==1  || datacarddongle== null) {
           formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
      if (mobilephonesim  ==1  || mobilephonesim== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
  if (medicalinsuranceterminated ==1  || medicalinsuranceterminated== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
            formContext.getAttribute("statuscode").setValue(5);
          }
}

  else if(casetitle.includes("Employee Development")){
      var deactivatexyme= formContext.getAttribute("pg_deactivatexyme").getValue();     
      var updatebonuseligibilityinbonusfile=formContext.getAttribute("pg_updatebonuseligibilityinbonusfile").getValue();
     
    if (deactivatexyme  ==1  || deactivatexyme== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
            formContext.getAttribute("statuscode").setValue(5);
          }
 if (updatebonuseligibilityinbonusfile ==1  || updatebonuseligibilityinbonusfile== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
            formContext.getAttribute("statuscode").setValue(5);
          }
  }
   else if(casetitle.includes("Learning & Development")){
    var completeactivetrainingsinlms= formContext.getAttribute("pg_completeactivetrainingsinlms").getValue();      
      var deactivatelmsaccess=formContext.getAttribute("pg_deactivatelmsaccess").getValue();
    
           if (completeactivetrainingsinlms  ==1  || completeactivetrainingsinlms== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
         if (deactivatelmsaccess==1  || deactivatelmsaccess== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
   }
   else if(casetitle.includes("Finance")){
       var updatetallywithenddate= formContext.getAttribute("pg_updatetallywithenddate").getValue();     
      var moveemployeefrompayrolltof=formContext.getAttribute("pg_moveemployeefrompayrolltoff").getValue();
      var receiveffclearancefromhr=formContext.getAttribute("pg_receiveffclearancefromhr").getValue();
      var processff= formContext.getAttribute("pg_processff").getValue();      
      var processgratuity=formContext.getAttribute("pg_processgratuity").getValue();
      var processleaveencashment=formContext.getAttribute("pg_processleaveencashment").getValue();
      var processdeductionsforlopadjustments=formContext.getAttribute("pg_processdeductionsforlopadjustments").getValue();
      var publishffpayslip=formContext.getAttribute("pg_publishffpayslip").getValue();
     var updatedateofexitinpfportal=formContext.getAttribute("pg_updatedateofexitinpfportal").getValue();
  
   if (updatetallywithenddate ==1  || updatetallywithenddate== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
            formContext.getAttribute("statuscode").setValue(5);
          }
 if (moveemployeefrompayrolltof ==1  || moveemployeefrompayrolltof== null) {
           formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
      if (receiveffclearancefromhr  ==1  || receiveffclearancefromhr== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
       if (processff ==1  || processff== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
               formContext.getAttribute("statuscode").setValue(5);
          }
        if (processgratuity  ==1  || processgratuity== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
      if (processleaveencashment ==1  || processleaveencashment== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
      if (processdeductionsforlopadjustments  ==1  || processdeductionsforlopadjustments== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
               formContext.getAttribute("statuscode").setValue(5);
          }
 if (publishffpayslip ==1  || publishffpayslip== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
               formContext.getAttribute("statuscode").setValue(5);
          }
 if (updatedateofexitinpfportal==1  || updatedateofexitinpfportal== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
   }
    else if(casetitle.includes("Employee Engagement")){
      var updatedeierglist= formContext.getAttribute("pg_updatedeierglist").getValue();     
      var updateclashoftitanshouselistjos=formContext.getAttribute("pg_updateclashoftitanshouselistjosh").getValue();
      var conductexitinterview=formContext.getAttribute("pg_conductexitinterview").getValue();
      var disabledojostoreaccess= formContext.getAttribute("pg_disabledojostoreaccess").getValue();      
      var cancelopenordersinthequeue=formContext.getAttribute("pg_cancelopenordersinthequeue").getValue();
     
          if (updatedeierglist  ==1 || updatedeierglist== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
             formContext.getAttribute("statuscode").setValue(5);
          }
      if (updateclashoftitanshouselistjos==1  || updateclashoftitanshouselistjos== null) {
           formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
           formContext.getAttribute("statuscode").setValue(5);
          }
 if (conductexitinterview  ==1  || conductexitinterview== null) {
           formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
            formContext.getAttribute("statuscode").setValue(5);
          }
        if (disabledojostoreaccess  ==1 || disabledojostoreaccess== null) {
            formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
            formContext.getAttribute("statuscode").setValue(5);
          }
        if (cancelopenordersinthequeue  ==1  || cancelopenordersinthequeue== null) {
             formContext.getAttribute("statuscode").setValue(1);
             }
            else  {
              formContext.getAttribute("statuscode").setValue(5);
          }
     }
   }
}
